#include "lexer.h"

using namespace std;

vector<shared_ptr<Token>> Lexer::tokenize(const string& code) {
    vector<shared_ptr<Token>> tokens;
    size_t line = 0;
    size_t position = 0;
    size_t i = 0;
    while (i < code.length()) {
        
    }
}
